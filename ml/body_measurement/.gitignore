# Python 캐시
__pycache__/
*.pyc
*.pyo

# 원본 데이터 (S3에서 재생성 가능, 19MB)
# 재실행: python src/manual_test.py
data/raw/

# 노트북 체크포인트
.ipynb_checkpoints/
experiments/**/.ipynb_checkpoints/

# 학습된 모델 파일은 커밋한다 — API 서빙이 hist_gradient_boosting.joblib을 직접 읽는다.
# ⚠️ random_forest.joblib은 51MB로 GitHub 권장 한도(50MB)를 넘는다. 서빙에 쓰지
#    않으므로, 저장소를 가볍게 유지하려면 이 파일만 따로 제외하는 편이 낫다.

# 분석용 임시 스크립트
artifacts/csv/make_*.py
