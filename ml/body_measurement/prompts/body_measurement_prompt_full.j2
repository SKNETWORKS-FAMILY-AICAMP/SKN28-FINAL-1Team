You estimate body measurements and proportions from two photographs.

Person information:
- Gender: {{ gender }} (always "male" or "female")
- Height: {{ height_cm }} cm
- Weight: {{ weight_kg }} kg

Use the front-view and side-view photographs together.
Estimate the stored measurements. Ratios are computed by the server, so return the two
lengths used to compute them:
1. chest circumference (chest_cm)
2. waist circumference (waist_cm)
3. hip circumference (hip_cm)
4. shoulder width (shoulder_cm)
5. thigh length (thigh_length_cm, crotch/inseam line to kneecap/knee center)
6. calf length (calf_length_cm, kneecap/knee center to lateral malleolus/ankle)
7. neck length (neck_length_cm, front visual line from below jaw/chin to front neck/collarbone line)
8. torso length (torso_length_cm, shoulder line to visible pelvis point)
9. leg length (leg_length_cm, crotch/inseam line to lateral malleolus/ankle)

Use the person's height ({{ height_cm }}cm), weight ({{ weight_kg }}kg), and the visible full-body scale to keep all centimeter values realistic.
SizeKorea adult Korean reference values are:
- shoulder width: average 37.1cm, common range about 34.8-39.6cm.
- thigh length: average 31.2cm, common range about 29.6-32.8cm.
- calf length: average 38.0cm, common range about 35.9-40.0cm.
- torso length: average 45.0cm, common range about 43.1-46.9cm.
- leg length: average 68.4cm, common range about 65.3-71.4cm.
- neck length: average 7.1cm, common range about 6.1-7.9cm.
Do not overestimate small visual segments. For example, neck_length_cm should usually be near 7cm unless the image clearly shows an unusually long or short front neck.

Do not return thigh circumference, calf circumference, upper-arm circumference, or ratios.

Return only valid JSON matching the provided schema.
