# 사진 기반 신체치수 추정 모델 평가 요약

## 1. 무엇을 평가했나

- 모델 입력: 정면 사진 1장, 측면 사진 1장, 성별, 키(cm), 몸무게(kg)
- 모델 예측: 가슴둘레, 허리둘레, 엉덩이둘레(cm)
- 정답값은 모델에 주지 않고, 호출 후에만 실제 신체치수와 비교함
- 평가 지표: 평균 절대 오차(MAE, cm) — 낮을수록 좋음 / 성공률 — 높을수록 좋음

## 2. 데이터 분할

| 구분       |  인원 | 용도                                                    |
| ---------- | ----: | ------------------------------------------------------- |
| Validation |  39명 | 모델·프롬프트 비교 및 후보 선정                        |
| Test       | 154명 | 최종 선택 모델의 한 번뿐인 최종 평가                    |
| 합계       | 193명 | SizeKorea와 기타 인물 데이터를 하나의 데이터셋으로 사용 |

- 이 실험은 별도 학습이 없는 API 기반 추론(Zero-shot)이므로, 일반적인 Train/Validation/Test 대신 `Validation → 최종 Holdout Test` 구조를 사용함
- Gemini는 Test 154명을 한 번 호출했지만 모델 선정·지표 비교에는 사용하지 않았고, 해당 예측 파일은 삭제함
- 따라서 Qwen·Kimi의 최종 Test 154명 평가는 아직 수행하지 않음

## 3. 실행 방식

- Gemini: `run_gemini.py`로 Gemini API 호출
- Qwen / Kimi / Grok: `run_openrouter.py`로 OpenRouter API 호출
- 각 호출의 예측값을 `*_predictions_*.csv`에 저장
- `evaluate_results.py`가 예측값과 정답지를 비교해 `*_evaluated_*.csv`, `*_metrics_*.json` 생성
- 모든 모델에 같은 Validation 39명, 같은 입력 정보, 같은 출력 JSON 형식을 적용

## 4. 후보 선정 기준

- 사진 2장과 키·몸무게·성별을 함께 이해하는 멀티모달 모델인지
- 가슴·허리·엉덩이 3개를 JSON으로 안정적으로 반환하는지
- 평균오차(MAE)가 낮은지
- 호출 성공률이 높은지
- 응답 시간과 이미지 포함 호출 비용이 프로젝트 범위에 맞는지

## 5. Validation 결과

| 모델                  |  성공 |    평균 MAE(cm) |        가슴 MAE | 허리 MAE |      엉덩이 MAE |   평균 시간(초) | 평균 호출비용(USD) |
| --------------------- | ----: | --------------: | --------------: | -------: | --------------: | --------------: | -----------------: |
| Gemini 3.5 Flash-Lite | 39/39 |           3.962 |           4.444 |    4.756 |           2.685 |           5.985 |             미기록 |
| Qwen 3.7 Flash        | 39/39 |           3.597 |           3.728 |    4.128 |           2.936 |           5.106 |           0.000152 |
| Kimi K2.5             | 39/39 | **2.757** | **2.944** |    3.159 | **2.167** |           7.653 |           0.004492 |
| Grok 4.3              | 39/39 |           3.441 |           3.803 |    3.062 |           3.459 | **1.747** |           0.005977 |

### 평균 MAE 비교 (낮을수록 좋음)

```text
Kimi K2.5              2.757 cm  ██████████████
Grok 4.3               3.441 cm  █████████████████
Qwen 3.7 Flash         3.597 cm  ██████████████████
Gemini 3.5 Flash-Lite  3.962 cm  ████████████████████
```

## 5-1. 서빙용 7개 프롬프트로 재측정 (Kimi K2.5, Validation 39명)

위 §5는 **가슴·허리·엉덩이 3개만** 묻는 프롬프트로 측정한 값이다. 실제 서비스는 7개를 모두
받아야 하므로 서빙용 프롬프트(`body_measurement_prompt_full.j2`)로 다시 측정했다.

| 부위 | 3개 프롬프트 | 7개 프롬프트 | 변화 |
| ------------ | ----: | ----: | ----: |
| 가슴 | 2.944 | 3.490 | +0.546 |
| 허리 | 3.159 | 3.205 | +0.046 |
| 엉덩이 | 2.167 | 2.377 | +0.210 |
| **평균** | **2.757** | **3.024** | **+0.267** |
| 평균 시간(초) | 7.653 | 9.614 | +1.961 |

- 39/39 성공, 신규 4개 부위(허벅지·종아리·팔뚝·어깨) 응답률 100%
- 사전에 정한 허용 범위(±0.3cm) 안이므로 7개 프롬프트를 서빙에 채택함
- 질문 항목을 늘리면 기존 3개 정확도가 소폭 희생된다는 점을 기록해 둔다

## 5-2. 최종 Test 154명 (Kimi K2.5 + 7개 프롬프트)

**154/154 성공, 실패 0건. 신규 4개 부위 응답률 100%.**

| 부위 | 전체 154명 | 정답 오류 제외 152명 |
| ------------ | ----: | ----: |
| 가슴 | 4.225 | **3.547** |
| 허리 | 3.156 | **3.149** |
| 엉덩이 | 2.742 | **2.759** |
| **평균** | 3.374 | **3.152** |
| 평균 시간(초) | 9.484 | — |

**정답 오류 2건을 제외한 152명 기준 평균 3.152cm가 실제 성능이다** (오류 상세는 §6 참조).

### Validation과의 비교

| 구간 | 인원 | 평균 MAE |
| ---------- | ----: | ----: |
| Validation | 39 | 3.024 |
| Test | 152 | 3.152 |

차이 0.128cm. 모델을 고른 Validation보다 Test가 소폭 나쁜 것은 정상적인 패턴이며,
과적합 징후가 없다는 뜻이다.

### 성별별 (정답 오류 제외 152명)

| 성별 | 인원 | 평균 MAE |
| ---- | ----: | ----: |
| 여성 | 74 | 3.236 |
| 남성 | 78 | 3.072 |

차이 0.164cm으로 성별 편향은 관찰되지 않는다.

### 오차 분포

| 지점 | 가슴 | 허리 | 엉덩이 |
| ------ | ----: | ----: | ----: |
| 중앙값 | 3.0 | 2.8 | 2.4 |
| 상위 25% 지점 | 5.1 | 4.2 | 3.6 |

절반 이상이 3cm 이내다.

## 5-3. 데이터셋 정책 변경 — SizeKorea만 사용 (2026-08-05 결정)

기존 Test 154명은 SizeKorea 145명 + 비-SizeKorea 외부 9명으로 구성돼 있었다.
외부 데이터는 동일한 실측 기준으로 검증되지 않았기 때문에 **최종 평가 기준은
SizeKorea 실측 데이터만 사용한다.** 외부 이미지, 라벨 행, split 행, VLM 실험 CSV 행은 삭제했다.

| 집합 | 인원 | 가슴 | 허리 | 엉덩이 | 평균 MAE |
| ------------------------- | ----: | ----: | ----: | ----: | ----: |
| SizeKorea만 | 145 | 4.252 | 3.194 | 2.643 | 3.363 |
| **SizeKorea − 정답오류 2건** | **143** | **3.532** | **3.187** | **2.660** | **3.126** |

1. **최종 기준은 SizeKorea 145명이다.** 외부 프로필 기반 정답은 평가 근거에서 제외한다.
2. **정답오류 2건을 제외한 참고 수치도 유지한다.** 발표·비교 시에는 어떤 기준을
   썼는지 함께 적는다.
3. SizeKorea-only 기준 split 비율은 Test 145명(80.11%) / Validation 36명(19.89%)로 유지된다.
4. 향후 Validation·Test 재구성 시에도 SizeKorea만 후보로 사용한다.

### VLM 응답의 비결정성 (2026-08-05 관측)

`temperature=0`으로 호출하지만 **같은 사진·같은 입력에도 응답이 달라질 수 있다.**
API 종단 시연에서 8/4 배치 결과와 대조한 결과다.

| 대상 | 8/4 배치 (가슴/허리/엉덩이) | 8/5 API | 일치 |
| ------------- | ----: | ----: | ---- |
| sizkorea_f111 | 88.5 / 76.0 / 94.0 | 85.0 / 72.0 / 91.0 | 전부 다름 |

f111은 같은 입력에서 MAE 1.17cm → 3.60cm로 벌어졌다. 단건 결과를 모델 성능의
근거로 쓰면 안 되고, 평가는 항상 표본 전체 평균으로 판단해야 한다는 뜻이다.
재현이 필요한 벤치마크는 응답을 CSV로 남겨 대조한다.

## 6. 검증 결과와 해석 시 유의사항

### ⚠️ 정답 데이터 오류 2건 (Test 154명, 미수정)

최종 Test에서 **정답지 자체가 잘못된 행 2건**을 발견했다. 모델이 틀린 것이 아니다.

| subject_id | 성별 | 키 | 몸무게 | 정답지 가슴 | 모델 예측 | 판정 |
| ---------- | ---- | ----: | ----: | ----: | ----: | ---- |
| `sizkorea_m163` | M | 170.9 | 54.5 | **8.2** | 82.5 | 사람 가슴둘레 8.2cm는 불가능. 자릿수 오류로 보임(82.x → 8.2) |
| `sizkorea_m158` | M | 177.9 | 77.8 | **57.3** | 94.5 | 177.9cm/77.8kg 남성의 가슴둘레로 비정상 |

- 이 2건이 가슴 MAE를 **3.547 → 4.225로 부풀렸다** (0.678cm 왜곡)
- Validation 36명에는 동일한 오류가 **0건**이다
- 판정 기준: 가슴·엉덩이 60cm 미만 또는 허리 40cm 미만을 성인 신체치수로 비상식적인 값으로 봄
- **아직 데이터를 수정하지 않았다.** 원본 SizeKorea 값을 확인한 뒤 고쳐야 하며,
  그 전까지 Test 지표는 "전체 154명"과 "정상 152명"을 함께 표기한다
- 재확인 방법: `data/splits/vlm/test_set.csv`에서 위 두 `subject_id`의 `chest` 열

### 확인 완료

- Validation 36명과 Test 145명은 중복 인원이 0명이고, 각 split 안의 중복 ID도 0명임
- 모든 대상자에게 정면·측면 이미지가 존재하며, 키·몸무게·성별·정답 신체치수가 누락되지 않음
  (다만 값이 존재한다고 해서 타당하다는 뜻은 아니다 — 위 오류 2건 참조)
- 모델 프롬프트에는 사진·성별·키·몸무게만 렌더링되고, 실제 가슴·허리·엉덩이 수치는 모델 호출 뒤 평가 단계에서만 사용됨
- 저장된 모든 모델 지표는 예측 CSV에서 다시 계산해 일치함
- 성별 분포: Validation F 19명 / M 20명, Test F 74명 / M 80명으로 균형이 유지됨

### 분할의 타당성

- 학습을 하지 않는 실험이므로 Validation 20%(39명)로 후보를 고르고 Test 80%(154명)로 최종 성능을 확인하는 구조는 타당함
- 다만 일반적인 학습 실험의 70/15/15 분할과는 목적이 다르므로, 문서에서는 `학습 없는 Zero-shot 모델 비교`라고 명시해야 함
- 엄격하게 모든 모델을 동일한 test로 비교해야 한다면 새로운 holdout set을 별도로 만들어야 함. 현재 Test 154명은 Qwen·Kimi에는 아직 사용되지 않았음

### 해석 한계와 보완할 점

- Kimi가 Qwen보다 평균 MAE가 0.841cm 낮았고, 같은 39명을 이용한 95% 부트스트랩 구간은 -1.418~-0.293cm로 Kimi 우세 방향이 유지됨
- Qwen과 Grok의 차이는 0.156cm이고 95% 구간이 -0.378~0.691cm라, 39명만으로 둘의 우열을 단정하기 어려움
- SizeKorea 외 데이터는 Validation 3명뿐이라 출처별 성능을 일반화할 수 없음
- API 모델은 제공자·시점에 따라 응답이 달라질 수 있으므로, 최종 후보는 10명 내외를 3회 반복 호출해 결과 흔들림도 확인하는 것이 좋음
- 전체 평균만으로는 부족하므로, 최종 Test에서는 성별·출처별 MAE와 큰 오차 사례도 함께 보고해야 함

## 6-1. ⚠️ 아직 없는 숫자 — 무사진 기준선 (2026-08-05 확인)

**이 문서에는 "사진을 써서 3.15cm"라는 결과만 있고, 같은 사람들을 사진 없이
성별·키·몸무게만으로 예측했을 때의 오차가 없다.** 그 숫자가 없으면 사진이 실제로
도움이 됐는지 판단할 근거가 없다.

- 1단계 tabular 보고서의 **1.915cm를 여기에 갖다 대면 안 된다.** 그건 SizeKorea 5,092명
  모집단에서 나온 값이고, 여기 평가셋은 별도 촬영 데이터셋이다. 모집단·측정 프로토콜이
  달라 차이가 사진 덕분인지 데이터가 쉬워서인지 구분되지 않는다
  (근거: `ml/docs/multimodal-model-selection.md` §0).
- 판정식: **같은 split 행에서** `MAE(VLM, 사진 사용)` < `MAE(tabular, 사진 없음)`.
- 산출 스크립트를 만들어 뒀다 — `src/no_photo_baseline.py`.

```powershell
cd ml\body_measurement

# ① 컬럼 정의 확정 — split의 chest가 SizeKorea의 `가슴둘레`인지 `젖가슴둘레`인지 판정
#    (두 항목은 평균 2.47cm 차이라 잘못 잡으면 모델 오차보다 큰 편향이 생긴다)
python src\no_photo_baseline.py --drop-impossible match

# ② 무사진 기준선 + 사진 有/無 비교
#    ①에서 확정한 매핑을 no_photo_baseline.py의 CONFIRMED_MAPPING에 반영한 뒤 실행
python src\no_photo_baseline.py --drop-impossible baseline
```

⚠️ **스크립트는 아직 한 번도 실행되지 않았다** (작성 세션에서 Python 실행이 권한으로
차단됨). 문법·동작 검증 전이므로 첫 실행 시 오류가 날 수 있다.

### ⚠️ 선결 과제 — 평가 라벨의 출처가 SizeKorea가 맞는지 확인

위 기준선 산출은 "SizeKorea에서 학습 → 평가셋 예측"이라 **두 쪽 측정 프로토콜이 같아야**
성립한다. 그런데 평가 라벨이 SizeKorea가 아닐 정황이 있다.

| 단서 | 관찰 |
| --- | --- |
| subject_id 형식 | `F010`, `F013`, `M163` — 데이터셋 20의 피험자 ID 체계 |
| 컬럼 | `front_camera_number=3`, `side_camera_number=12` — 카메라 32각도는 데이터셋 20 개념 |
| 사진 유무 | SizeKorea 8차 직접측정 시트에는 **이미지가 아예 없다** |
| 대조 | `data/processed/sizekorea_measurements_clean.csv`에 F010(167.1cm/52.3kg)·F013(152.0cm/74.0kg)과 일치하는 행이 **없다** |

즉 `sizkorea_*` 접두사와 `source=sizkorea` 값에도 불구하고, 실제 출처는
**`s3://skn28-cozy/20.한국인_전신_형상_및_치수_측정_데이터/`일 가능성이 높다.**
(로컬 대조까지만 한 결과이므로 S3 원본과 맞춰 확정해야 한다.)

확정 방법 — 데이터셋 20의 같은 ID 원본과 값을 대조한다.

```text
s3://skn28-cozy/20.한국인_전신_형상_및_치수_측정_데이터/01.데이터/
  1.Training/원천데이터_230524add/신체정보데이터/F010.csv
→ 키 167.1 / 몸무게 52.3 / 젖가슴둘레 86.1 이 일치하는지 확인
```

**맞다면 따라오는 것**

1. 라벨의 `chest`는 `젖가슴둘레`다. SizeKorea의 `가슴둘레`와 평균 2.47cm 차이나므로,
   기준선 계산에서 chest를 그대로 쓰면 안 된다 → `match`를 먼저 돌려 확정할 것
   (`no_photo_baseline.py`의 `CONFIRMED_MAPPING`은 chest·shoulder를 일부러 빼 뒀다).
2. §5-3의 "SizeKorea 실측만 사용" 정책은 **이름 기준으로 세워진 것**이라 재검토가 필요하다.
3. 파일·컬럼의 `sizkorea` 표기를 실제 출처에 맞게 고쳐야 혼동이 재발하지 않는다.

## 7. 결론과 다음 순서

- 1순위 운영 후보: **Qwen 3.7 Flash**

  - 39/39 성공, Kimi보다 약 30배 저렴, 오차도 경쟁력 있음
- 정확도 확인 후보: **Kimi K2.5**

  - 평균오차가 가장 낮음
  - 출력 길이 초과 1건(`sizkorea_m454`)을 재시도하여 39/39 성공을 확인함
- Grok 4.3: 가장 빠르지만 Qwen보다 비용이 높고 평균오차 이점이 작음
- Gemini 3.5 Flash-Lite: 이번 Validation에서는 Qwen·Kimi보다 우선순위가 낮음
- 다음 단계

  - **무사진 기준선부터 산출한다 (§6-1).** 사진이 이득인지 확인되기 전에 모델을 확정하면,
    더 정확한 tabular 경로를 두고 더 비싸고 느린 사진 경로를 고르는 결과가 될 수 있다
  - **OpenRouter 크레딧을 채우고 kimi-k2.5·grok-4.3만 182명 재실행한다 (§8).**
    현재 이 둘은 미평가 상태다. 완주한 3개 모델은 다시 돌릴 필요 없다
  - qwen3-vl-8b의 JSON 파싱 실패 14건 원인 확인 (출력 계약 점검)
  - Qwen과 Kimi 중 최종 모델 1개 선택
  - 선택한 모델만 Test 154명에 실행하고, 예측 파일·평가 파일·지표 파일을 남김

## 8. 6모델 × 182명 배치 결과 (`openrouter-182-test`)

근거: `experiments/vlm/_summaries/openrouter-182-test/all_models_summary.csv`,
모델별 원본은 `experiments/vlm/{모델}/openrouter-182-test/predictions.csv`.
(`reports/vlm_openrouter_182/`에는 실행 로그만 남아 있고, 산출물은 `experiments/`로 재편됐다.)

| 모델 | 성공 | 가슴 MAE | 허리 MAE | 엉덩이 MAE | 3부위 평균 | 평균 지연(초) |
| ----------------- | -------: | -----: | -----: | -----: | -----: | -----: |
| gemini-flash | **182/182** | 3.824 | 3.424 | 2.779 | **3.342** | 1.37 |
| gemini-flash-lite | **182/182** | 3.745 | 3.036 | 3.307 | 3.363 | 2.06 |
| gemma-3-12b | **182/182** | 4.470 | 5.208 | 3.253 | 4.310 | 1.19 |
| qwen3-vl-8b | 168/182 | 5.942 | 7.590 | 4.590 | 6.041 | 1.08 |
| kimi-k2.5 | 7/182 ⚠️ | (2.300) | (4.586) | (3.000) | — | 72.27 |
| grok-4.3 | 0/182 ⚠️ | — | — | — | — | — |

⚠️ Kimi·Grok의 실패는 **OpenRouter 크레딧 소진**이다 (아래 참조). 모델 성능이 아니다.

### 실패 원인 — 모델 문제가 아니라 크레딧 소진

`predictions.csv`의 `error` 컬럼을 집계한 결과다. **Kimi·Grok의 실패는 모델이 거부하거나
못 푼 것이 아니다.** 실행 순서상 뒤쪽 두 모델에서 OpenRouter 크레딧이 떨어졌다.

| 모델 | 실패 | 원인 내역 |
| ----------- | ----: | --- |
| kimi-k2.5 | 175 | HTTP 402 크레딧 부족 **173건** + 파서 버그(`'NoneType' object has no attribute 'find'`) 2건 |
| grok-4.3 | 182 | HTTP 402 크레딧 부족 **182건** (한 번도 호출되지 못함) |
| qwen3-vl-8b | 14 | JSON 파싱 실패 `Expecting value: line 1 column 1` 14건 |

**따라서 이렇게 읽어야 한다**

1. **Kimi·Grok은 이 배치에서 평가되지 않은 것이다.** "실패율이 높은 모델"이 아니다.
   §2의 1차 게이트(거부·회피율)로 탈락시키면 잘못된 판단이 된다. 크레딧을 채우고
   **재실행해야 비교가 성립한다.**
2. Kimi의 가슴 2.300cm는 **7명에서 나온 값이라 지표로 쓸 수 없다.** 평균 지연 72.27초도
   402 에러 응답이 섞인 값이라 실제 추론 시간이 아니다.
3. **실제로 182명을 완주한 모델은 gemini-flash / gemini-flash-lite / gemma-3-12b 셋뿐이고,**
   앞의 둘이 3.34~3.36cm로 사실상 동률이다. 이 셋 사이의 비교만 현재 유효하다.
4. **qwen3-vl-8b의 14건은 진짜 모델 쪽 실패다** — 응답이 JSON이 아니어서 파싱이 깨졌다.
   §2 기준 #3(출력 계약)에 걸리는 사례이므로 프롬프트·스키마 강제를 점검할 것.
5. `qwen3-vl-8b`는 §5의 `Qwen 3.7 Flash`와 **다른 모델**이다. 두 표를 같은 줄에 놓고
   비교하면 안 된다.

**§7의 "1순위 Qwen / 정확도 후보 Kimi"는 아직 뒤집히지 않았다.** 다만 그 근거는 여전히
Validation 39명뿐이고, 182명 배치에서 Kimi는 사실상 미평가 상태다.
