# indexer 의존성.
# torch는 CUDA 빌드가 환경(RunPod 베이스 이미지 등)에 이미 있으면 그대로 쓴다.
# 없는 환경에서는 https://pytorch.org 안내에 따라 CUDA 버전에 맞게 먼저 설치할 것.
torch>=2.1
open_clip_torch>=2.24
qdrant-client>=1.9
boto3>=1.34
Pillow>=10.0
python-dotenv>=1.0
tqdm>=4.66
