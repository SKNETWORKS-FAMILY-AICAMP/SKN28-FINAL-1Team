# product-indexer(쇼핑 상품 임베딩 worker) 의존성.
# 공용 이미지 임베더(util/embedder.py) 의존성은 util/requirements.txt에 있다.
-r ../util/requirements.txt

# BGE-M3 한국어 텍스트 임베딩.
# 주의: transformers 4.5x의 torch>=2.6 강제(CVE-2025-32434)는 .bin 체크포인트에만
# 적용된다. bge_embedder가 use_safetensors=True로 safetensors를 강제하므로
# 베이스 이미지의 torch 2.3.1에서도 로드된다.
sentence-transformers>=3.0,<4
# Qdrant 적재
qdrant-client>=1.9
# S3 상품 이미지 보존
boto3>=1.34
# 이미지 검증·정규화
Pillow>=10.0
# catalog 내부 API 호출 / 외부 이미지 다운로드
requests>=2.31
python-dotenv>=1.0
